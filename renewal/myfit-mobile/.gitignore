# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment variables (블랭킷 + 현재 추적 4파일 명시 예외 — CI 빌드가 NEXT_PUBLIC_* 플래그로 실사용, git rm --cached 금지)
.env*
!.env.*.example
!.env.app
!.env.app.demo
!.env.demo
!.env.production

# Signing credentials (CISO — 평문/키스토어 커밋 절대 금지)
android/keystore.properties
# 릴리스 서명 키스토어 — 커밋 금지(노출 시 서명키 오염). 로컬/CI-secret으로만 주입.
*.keystore
*.jks
android/release.keystore
android/app/release.keystore

# OS
.DS_Store
Thumbs.db
